t=input()
for j in range(0,t):
    s=raw_input()
    s=s.split()
    n=int(s[0])
    k=int(s[1])
    max=0
    for i in range(1,k+1):
        if(n%i>max):
            max=n%i
    print max
